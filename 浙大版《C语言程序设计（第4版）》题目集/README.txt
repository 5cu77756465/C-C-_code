这个文件夹存粹是为了做浙大版《C语言程序设计（第4版）》题目集而创建的，
但在Dev下一个project只能有一个包含main方法的c程序
不然会编译出错（我查看了自动生成的Makefile文件,它的固定格式会导致冲突）
这里我直接是做一题移除一个c程序来保证编译不会出错
这想来也是在IDE编程的不便之处之一，我还是喜欢在Linux编程

ex2-11这里是我在未打开项目时保存的，它自动生成了一个exe文件，且没有.o文件
